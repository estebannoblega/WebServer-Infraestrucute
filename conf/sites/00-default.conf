# DEFAULT HTTP PAGE

server {
    listen 80 default_server;
    server_name _;

    return 444;
}


# DEFAULT HTTPS PAGE

server {
    listen 443 ssl default_server;
    server_name _;

    ssl_reject_handshake on;
}